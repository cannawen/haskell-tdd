module NQueensSpec (spec) where

import Test.Hspec
import Lib (listToArray)

import NQueens51 (calculateNQueens)

spec :: Spec
spec = do
    -- describe "n Queens" $ do
    --     it "example 1" $ do
    --         calculateNQueens 4 `shouldBe` 2
    --     it "example 2" $ do
    --         calculateNQueens 1 `shouldBe` 1

